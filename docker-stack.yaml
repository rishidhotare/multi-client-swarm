version: "3.9"

networks:
  app-network:
    driver: overlay

services:

  traefik:
    image: traefik:v2.10
    command:
      - --providers.docker.swarmMode=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
    ports:
      - 80:80
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - app-network
    deploy:
      placement:
        constraints:
          - node.role == manager

  client-a:
    image: client-a:latest   # ✅ FIXED
    networks:
      - app-network
    secrets:
      - client_a_db
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        parallelism: 1
        delay: 10s
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.client-a.rule=Host(`client-a.local`) || Host(`43.204.218.55`)"
        - "traefik.http.services.client-a.loadbalancer.server.port=3000"

  client-b:
    image: client-b:latest   # ✅ FIXED
    networks:
      - app-network
    secrets:
      - client_b_db
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        parallelism: 1
        delay: 10s
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.client-b.rule=Host(`client-b.local`) || Host(`43.204.218.55`)"
        - "traefik.http.services.client-b.loadbalancer.server.port=8000"

secrets:
  client_a_db:
    external: true
  client_b_db:
    external: true
